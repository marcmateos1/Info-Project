class NavPoint:
    def __init__(self, number, name, latitud, longitud):
        self.number=int(number)
        self.name=name
        self.latitud=float(latitud)
        self.longitud=float(longitud)
